"""Content Studio content services.

Stages that turn a topic brief into structured inputs every later
pipeline (pitches, narrative, draft, review, speech) builds on. The
two services here consume the provider abstractions from
``studio.providers``; later stages consume the typed outputs.
"""
